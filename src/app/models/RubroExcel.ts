export class RubroExcel {
    id: number;
    nombre: string;
    descripcion: string;
    constructor(id: number, nombre: string, decripcion: string) {
        this.id = id;
        this.nombre = nombre;
        this.descripcion = decripcion;
    }
}